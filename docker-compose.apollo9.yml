# Profoundd Search Engine - Production (Apollo9)
# Usage: docker compose -f docker-compose.apollo9.yml up -d --build (or: make up-apollo9)

services:
  web:
    build: .
    container_name: profoundd
    ports:
      - "3004:5000"
    env_file:
      - .env.prod
    environment:
      - ELASTICSEARCH_URL=http://elasticsearch:9200
      - OLLAMA_URL=http://10.0.6.1:11434
    # SearXNG lives in another compose stack on the host. host.docker.internal
    # gives the maps-tab web fallback a stable hostname that survives restarts.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - profoundd-data:/app/data
    depends_on:
      elasticsearch:
        condition: service_healthy
    restart: unless-stopped

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.1
    container_name: profoundd-es
    hostname: profoundd-es
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms4g -Xmx4g"
      - path.repo=/usr/share/elasticsearch/snapshots
    ports:
      - "127.0.0.1:9201:9200"
      - "100.117.127.32:9201:9200"  # Tailscale — allows Azure7 indexer to reach ES
    volumes:
      - profoundd-es-data:/usr/share/elasticsearch/data
      - profoundd-es-snapshots:/usr/share/elasticsearch/snapshots
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health | grep -q '\"status\":\"\\(green\\|yellow\\)\"'"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    restart: unless-stopped

volumes:
  profoundd-data:
  profoundd-es-data:
  profoundd-es-snapshots:
